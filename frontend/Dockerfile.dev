FROM node:20-alpine

WORKDIR /app

# 依存関係のみ先にコピーしてインストール（キャッシュ活用）
COPY package*.json ./
RUN npm ci

# ソースはボリュームマウントするので COPY 不要
# ホスト 0.0.0.0 でリッスンしてコンテナ外からアクセス可能にする
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
