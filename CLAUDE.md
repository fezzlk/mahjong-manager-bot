# Claude Code ルール（mahjong-manager-bot）

## AskUserQuestion ツールの使用ルール

以下のケースでは **必ず** AskUserQuestion ツールを使用してユーザーに確認すること：

### 使用すべき場面

1. **要件・意図が曖昧な場合**
   - タスクの目的や期待する結果が不明確なとき
   - 複数の解釈が可能なリクエストを受けたとき

2. **複数のアプローチが存在する場合**
   - 実装方法が複数あり、どれを選ぶかユーザーの好みに依存するとき
   - 例: ライブラリの選択、アーキテクチャパターンの選択

3. **破壊的・不可逆な操作の前**
   - ファイルの削除、ブランチの削除、データベースの変更など
   - ただし EnterPlanMode 経由での確認が適切な場合はそちらを優先

4. **大きな変更を伴う実装の前**
   - 3ファイル以上にまたがる変更
   - 既存の動作や構造に影響する変更
   - ※ この場合は EnterPlanMode を先に使い、計画承認後に実装する

5. **ユーザーの好みが影響する場合**
   - コミットメッセージのスタイル、命名規則など、好みが分かれる事項
   - 以前の会話でユーザー好みが確認されていない事項

6. **ツール実行がブロックされた・拒否された場合**
   - ユーザーがツール実行を拒否した理由が不明なとき
   - 代替アプローチを探るとき

### 使用しなくてよい場面

- 明らかに単純で一意な修正（タイポ修正、自明なバグ修正）
- ユーザーが詳細な指示を与えており、判断の余地がないとき
- 計画承認のため（その場合は ExitPlanMode を使う）
- リサーチ・調査タスク（ファイル読み込み、コード検索など）

### 注意

- 「計画はこれでよいですか？」という確認には **AskUserQuestion ではなく ExitPlanMode** を使う
- オプションは 2〜4 個、明確で相互排他的なものにする
- 質問は簡潔・具体的に、疑問符で終わらせる

---

## 出力ファイルのルール

調査報告・資料・スクリプトなどの出力が発生する場合は、以下のパスに保存する。

**パス:** `~/ai-output/mahjong-manager-bot/`

**ファイル名形式:** `yyyyMMddHHmmss_<説明的なファイル名>.<拡張手>`
（yyyyMMddHHmmss はファイル生成日時）

**サブディレクトリ:**

| ディレクトリ | 用途 |
|-------------|------|
| `report/` | コードレビューや調査報告。時刻を意識した時点の状態を記録するもの |
| `docs/` | 資料。時間変化が少ない情報（設計思想・設計・概念・仕様など） |
| `idea/` | 提案レベルの出力 |
| `sql/` | 作業で発生したSQL（作業の再現性のため） |
| `scripts/` | 作業で発生したスクリプト（再現性・復帰・流用のため） |
| `tmp/` | 一時的な中間データ（大規模作業での中間データ・中断時の復帰用） |
| `other/` | 上記のどれにも該当しないもの |
