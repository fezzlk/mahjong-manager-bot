# DB設計レビュー（要件起点 / 現行運用起点）

## 目的
本ドキュメントは、麻雀マネージャーボットのDB設計について以下2パターンで整理する。

1. 要件起点（現状の操作方法を考慮しない）
2. 現行運用起点（mode切替・逐次更新・精算コマンド運用を前提）

加えて、各パターンを実現する際に MongoDB / PostgreSQL のどちらが適しているかを示す。

---

## 現状の要点（ソースから確認した事実）
- コレクションは `line_users`, `groups`, `group_settings`, `matches`, `hanchans`, `user_matches`, `user_hanchans`, `user_groups` ほか。
- `match` / `hanchan` は `Dict[line_user_id]` 形式の可変マップでスコアを保持。
- 集計はDB内ではなくアプリ側ループで実施。
- 半荘確定時に複数コレクションを連続更新するが、トランザクション境界は明示されていない。
- 重複防止は主に「findしてからinsert」のアプリ実装で、DB制約ベースではない。

---

## 1. 要件起点（操作方法を考慮しない）

### 要件の解像度
「LINEチャット経由で、各プレイヤーが半荘成績を入力し、登録・参照・比較できる」ことが中核。

### あるべきDB設計（論理）
- `players`（LINEユーザー）
- `groups`（対戦ルーム）
- `group_memberships`（group-player関係）
- `matches`（対戦セッション）
- `hanchans`（match配下の半荘）
- `hanchan_scores`（hanchan-player単位の明細）
- `match_settlements`（精算結果。保存するなら再計算可能性を担保）

### 現状との差異
1. スコア明細が行データでなく可変Map保管で、制約・集計・監査に弱い。
2. 派生値（合計点/精算額）を本体ドキュメントに持ち、再計算と整合性管理が難しい。
3. ユーザー参照キーが `user_id(ObjectId)` と `line_user_id(str)` で混在し、参照整合性が弱い。
4. ユニーク性がDB制約でなくアプリ判定依存。

### このパターンのDB選定
- **推奨: PostgreSQL**
- 理由:
  - 正規化設計と整合性制約（FK/UNIQUE/CHECK）を素直に実装できる。
  - 集計/比較（期間、順位、累計）をSQLで安定して表現できる。
  - 仕様拡張時に「正しいデータを保つ」基盤として強い。
- MongoDBを選ぶ場合:
  - 初期実装は速いが、要件中心設計ではデータ品質担保をアプリ実装に寄せる必要が大きい。

---

## 2. 現行運用起点（mode切替・逐次更新を前提）

### 運用の解像度
- `_input` で入力開始。
- 各プレイヤーの投稿ごとに半荘データ更新。
- `_tobi` や `_chip_ok` を経由し、`_finish` で精算。
- 途中状態（入力中）と確定状態（精算済み）を行き来する。

### あるべきDB設計（論理）
- `group_runtime_state`（mode, active_match_id, active_hanchan_idを一貫管理）
- `hanchan_score_drafts`（入力途中の点数）
- `hanchan_scores`（確定点数）
- `match_settlements`（確定時スナップショット）
- `events_audit_log`（修正/削除/再精算の履歴）
- 必要なら `read_models`（ランキング等の参照最適化）

### 現状との差異
1. 状態管理が `group.mode` と `active_*_id`、`status` に分散。
2. 途中データと確定データの境界が弱く、再実行・復旧フローが設計として明文化されていない。
3. 複数コレクション更新に対する原子性の担保が弱い。
4. 管理画面からの直接更新時に、派生データとの同期保証が弱い。
5. 監査ログがなく、運用トラブル時の追跡性が低い。

### このパターンのDB選定
- **推奨: PostgreSQL（第一候補）**
- 理由:
  - 入力中→確定の状態遷移をトランザクションで扱いやすい。
  - Draft/Final/Eventの境界をテーブルで明示しやすい。
  - 将来の運用監査（誰がいつ変更したか）を実装しやすい。
- **MongoDBが有利な条件**:
  - 「現状構造をほぼ保った短期移行」が最優先。
  - スキーマ再設計よりも改修コスト最小化を優先。
  - ただし長期的には整合性・監査性・分析性で設計負債が残りやすい。

---

## 結論（2パターン共通）
- 中長期で設計を見直すなら、**両パターンとも PostgreSQL 推奨**。
- MongoDBは「現行コード温存での短期延命」には有効だが、今回のようにDB設計そのものを見直す目的には適合度が下がる。

---

## 補足（移行方針の実務案）
1. 先に論理モデル（ER）を確定。
2. `line_user_id` を全体の業務キーとして統一し、内部IDは補助キーにする。
3. Draft/Final/Eventを分離し、精算は再計算可能な設計にする。
4. 最後にランキング等を read model 化して性能要件を満たす。
